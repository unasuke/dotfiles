# sshfwd helper relies on ControlMaster so the client-side `sshfwd <host> <port>`
# can attach a port forward to the existing session with `ssh -O forward`.
Host *
    ControlMaster auto
    ControlPath ~/.ssh/cm-%r@%h:%p
    ControlPersist 10m
